services:
  - type: web
    name: estrategia-electoral
    runtime: static
    buildCommand: echo "No build needed"
    staticPublishPath: ./analisis
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
